# <ModuleName> — Architecture (current implementation)

Describes the *current* build of this module. Expected to change if this module is ported to a
different stack — see `../../decisions/` for why current choices were made.

## Internal Structure

- <how the code is organized — layers, key files/folders, notable internal components>.

## Tech Stack

- Language / runtime / version: <e.g. C# / .NET 10>.
- Framework(s): <e.g. ASP.NET Core Minimal APIs>.
- Key libraries and why each was chosen: <library — reason, with an ADR link if the choice was
  non-obvious>.

## Deployment

- <how it's run/hosted, dev URL or entry point, and how it relates to the other modules — see
  `../../architecture/overview.md`>.

## Dependencies

- <other modules this one depends on, or "none">.
- <external services or databases, or "none in this phase">.
