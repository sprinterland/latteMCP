# Task Log

Index of **open** Task Records only (mirrors `../../PLAN.md`'s "active/near-term work, not a
historical record" framing) — see `tasks/README.md` for what a Task Record is, the full file
layout, and the status enum. Once a task closes (Auditor writes `audit.md` with a clean verdict,
or Reviewer/Tester escalates it to a terminal state), move its row to `completed_tasks.md` instead
of deleting it here.

| Task | Filed | Classification | Status | Owner-role |
|---|---|---|---|---|
| `TASK-<id>-<slug>` | `<YYYY-MM-DD>` | `<minor / major / — pending Analyst>` | `<latest status.jsonl status>` | `<role currently holding the claim, or "—" if unclaimed>` |

_Nothing filed yet._
